// @ts-check

/**
 * Playwright config placeholder.
 *
 * Browser-visible repositories must fail tests on unexpected console warnings
 * and errors. Add the console listener in test setup before enabling real tests.
 *
 * @type {{ testDir: string }}
 */
export default {
  testDir: "./test"
};
