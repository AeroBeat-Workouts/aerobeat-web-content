{
  "name": "@aerobeat/web-content-testbed",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "demo": "echo \"Add a repo-local Vite or static demo command when content runtime scenes need serving.\"",
    "serve": "echo \"Define host, port, cache busting, QR/link output, served roots, and secure-context handling before mobile validation.\"",
    "test": "echo \"Add content package and variant fixture tests during Task 5.\"",
    "test:browser": "echo \"Add Playwright browser tests for component-composed content runtime scenes during Task 5.\""
  }
}
